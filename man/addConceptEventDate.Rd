% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/addConceptEvent.R
\name{addConceptEventDate}
\alias{addConceptEventDate}
\title{Add the first or last concept event and its date}
\usage{
addConceptEventDate(
  x,
  conceptSet,
  indexDate = "cohort_start_date",
  censorDate = NULL,
  targetDate = "event_start_date",
  order = "first",
  window = list(c(0, Inf)),
  multipleEvents = NULL,
  nameStyle = "{value}_{window_name}",
  name = NULL
)
}
\arguments{
\item{x}{A table containing individuals in a CDM reference.}

\item{conceptSet}{A named list of concept sets.}

\item{indexDate}{Name of a date column in \code{x}, or a single date to use for
all rows, used as the reference date.}

\item{censorDate}{Date or name of a date column in \code{x} on which to censor
follow-up. If \code{NULL}, no censoring is applied.}

\item{targetDate}{Name or names of date columns in the target tables to use
for the intersection.}

\item{order}{Which record to use when multiple records occur in a window:
\code{"first"} or \code{"last"}.}

\item{window}{Window or windows of time relative to \code{indexDate} to consider.}

\item{multipleEvents}{How events occurring on the same date are handled. If
\code{NULL}, the first event in the original event order is returned. If \code{TRUE},
all simultaneous event names are sorted alphabetically and joined with
\code{"; "}. A character vector gives priority to the specified event names; the
first matching name is returned, with unspecified names following in
alphabetical order. Boundary labels (\code{"censor"} and
\code{"end_of_observation"}) are never combined with event names.}

\item{nameStyle}{Naming pattern for the added columns. It must contain
\code{{value}} and can also contain \code{{window_name}}.}

\item{name}{Name of the new table. If \code{NULL}, a temporary table is returned.}
}
\value{
\code{x} with an event column and a date column for every window.
}
\description{
\code{addConceptEventDate()} finds the first or last event from a set of
concepts in each window. When no event is observed before the applicable
boundary, the event is reported as \code{"end_of_observation"} if the observation
period boundary is reached or \code{"censor"} if the window boundary or
\code{censorDate} is reached. The date value represents that boundary.
}
\examples{
\donttest{
library(PatientProfiles)

cdm <- mockPatientProfiles(source = "duckdb")

cdm$cohort1 |>
  addConceptEventDate(conceptSet = list(acetaminophen = 1125315L))
}

}
